name: Data Scraping and Upload

on:
  schedule:
    - cron: '0 0 * * *'  # Runs daily at midnight UTC
  workflow_dispatch:  # Allow manual triggering of the workflow as well

jobs:
  upload-to-drive:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout the repository
        uses: actions/checkout@v3.1.0  # Update to v3 (latest version)

      - name: Set up Python
        uses: actions/setup-python@v3.1.0  # Update to v3 (latest version)
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Create credentials file
        env:
          GOOGLE_APPLICATION_CREDENTIALS: ${{ secrets.GOOGLE_APPLICATION_CREDENTIALS }}
        run: |
          echo "$GOOGLE_APPLICATION_CREDENTIALS" > credentials.json

      - name: Run Python script
        env:
          GOOGLE_APPLICATION_CREDENTIALS: $PWD/credentials.json
        run: python main.py
